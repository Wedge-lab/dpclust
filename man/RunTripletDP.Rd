% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triplet_functions.R
\name{RunTripletDP}
\alias{RunTripletDP}
\title{Main entry point for the triplet pipeline. This pipeline is used when a donor has more
samples than the native nD pipeline can practically cluster jointly. Work is split into
three steps, mirroring RunDP's analysis_type dispatch: "core" (step 1, run the DP core
algorithm on all subsamples), "density" (step 2, estimate density for one triplet of
subsamples - to be run once per possible triplet) and "combine" (step 3, combine the
density estimates of all triplets into a single consensus).}
\usage{
RunTripletDP(
  step,
  sample_params,
  triplet_params,
  outdir,
  advanced_params = NULL,
  subsample.indices = NULL
)
}
\arguments{
\item{step}{One of "core", "density", "combine"}

\item{sample_params}{List with sample parameters (see make_sample_params). Required for all steps}

\item{triplet_params}{List with triplet run parameters (see make_triplet_params). Required for all steps}

\item{outdir}{Directory where the output will be saved}

\item{advanced_params}{List with advanced parameters (see make_advanced_params). Only used by step "core"}

\item{subsample.indices}{The indices (within sample_params$subsamples) of the subsamples that make up this triplet. Required for step "density", not used otherwise}
}
\description{
Main entry point for the triplet pipeline. This pipeline is used when a donor has more
samples than the native nD pipeline can practically cluster jointly. Work is split into
three steps, mirroring RunDP's analysis_type dispatch: "core" (step 1, run the DP core
algorithm on all subsamples), "density" (step 2, estimate density for one triplet of
subsamples - to be run once per possible triplet) and "combine" (step 3, combine the
density estimates of all triplets into a single consensus).
}
\author{
sd11
}
